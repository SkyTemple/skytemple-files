
def 0 {
    debug_Print('In which the Happiness Machine marches on.');
    CallCommon(CORO_SUBSCREEN_INIT);
    screen2_FadeIn(1, 0);
    CallCommon(CORO_DEBUG_FLAG_SETTING_FUTURE);
    message_Mail("No one will tell me that I have done my best.[K]\nI do not know if this would be true.[K]\nI do not know if that would help.[K]\n\nI know nothing at this point.[C][VS:1:2][hero] and [partner] are marching on.[K]\n[VS:1:4]I can only wait.[VR]");
    message_Close();
    CallCommon(CORO_DEBUG_FLAG_SETTING_PUPURIN);
    end;
}
